<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.music_backend.domain.user.UserMapper">

    <resultMap id="UserMap" type="org.example.music_backend.domain.user.User">
        <id property="userId"               column="user_id" />
        <result property="oauthType"        column="oauth_type" />
        <result property="providerId"       column="provider_id" />
        <result property="email"            column="email" />
        <result property="fullName"         column="full_name" />
        <result property="nickname"         column="nickname" />
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO user_tb
        (oauth_type, provider_id, email,full_name, nickname)
        VALUES (#{oauthType}, #{providerId}, #{email},#{fullName}, #{nickname})
    </insert>

    <select id="findByEmail" resultMap="UserMap">
        select
            user_id,
            oauth_type,
            provider_id,
            email,
            full_name,
            nickname
        from
            user_tb
        where
            email = #{email}
    </select>

    <select id="findById" resultMap="UserMap">
        select
            user_id,
            oauth_type,
            provider_id,
            email,
            full_name,
            nickname
        from
            user_tb
        where
            user_id = #{userId}
    </select>

    <select id="findByNickname" resultMap="UserMap">
        select
            user_id,
            oauth_type,
            provider_id,
            email,
            full_name,
            nickname
        from
            user_tb
        where
            nickname = #{nickname}
    </select>
</mapper>
