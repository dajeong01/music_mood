<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.music_backend.domain.user.SpotifyTokenMapper">

    <insert id="insertSpotifyToken">
        INSERT INTO spotify_user_token (user_id, access_token, refresh_token)
        VALUES (#{userId}, #{accessToken}, #{refreshToken});
    </insert>

</mapper>